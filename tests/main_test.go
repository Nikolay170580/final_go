package tests

import (
	"os"
	"testing"
)

func TestMain(m *testing.M) {
	// Инициализация токена перед запуском тестов, если включена авторизация
	// Создаём мок-тестинг для вызова хелпера без реального *testing.T
	// Токен будет получен при первом вызове защищённого эндпоинта через EnsureToken
	
	exitCode := m.Run()
	
	// Очистка токена после тестов (опционально)
	Token = ""
	
	os.Exit(exitCode)
}